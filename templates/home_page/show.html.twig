{% extends 'base.html.twig' %}

{% block title %}HomePage{% endblock %}

{% block body %}
    <h1>HomePage</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ home_page.id }}</td>
            </tr>
            <tr>
                <th>PageTitle</th>
                <td>{{ home_page.pageTitle }}</td>
            </tr>
            <tr>
                <th>WelcomeText</th>
                <td>{{ home_page.welcomeText }}</td>
            </tr>
            <tr>
                <th>AboutTitle</th>
                <td>{{ home_page.aboutTitle }}</td>
            </tr>
            <tr>
                <th>AboutText</th>
                <td>{{ home_page.aboutText }}</td>
            </tr>
            <tr>
                <th>SectionDishesTitle</th>
                <td>{{ home_page.sectionDishesTitle }}</td>
            </tr>
            <tr>
                <th>SectionDishesText</th>
                <td>{{ home_page.sectionDishesText }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_home_page_index') }}">back to list</a>

    <a href="{{ path('app_home_page_edit', {'id': home_page.id}) }}">edit</a>

    {{ include('home_page/_delete_form.html.twig') }}
{% endblock %}
