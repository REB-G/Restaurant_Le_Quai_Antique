<form method="post" action="{{ path('app_menus_delete', {'id': menu.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ menu.id) }}">
    <div class='flex-center menus-form__modification'>
        <button class="btn menus-form__modification--button">Supprimer</button>
    </div>
</form>
