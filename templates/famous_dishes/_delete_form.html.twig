<form method="post" action="{{ path('app_famous_dishes_delete', {'id': famous_dish.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ famous_dish.id) }}">
    <button class="btn">Delete</button>
</form>
