{% extends 'base.html.twig' %}

{% block title %}Le Quai Antique - Récapitulatif des plats phares{% endblock %}

{% block body %}
    <h1 class='famous-dishes-form__title'>Récapitulatif des plats phare.</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ famous_dish.id }}</td>
            </tr>
            <tr>
                <th>Nom du plat</th>
                <td>{{ famous_dish.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ famous_dish.description }}</td>
            </tr>
            <tr>
                <th>ImageName</th>
                <td>{{ famous_dish.imageName }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_famous_dishes_index') }}">Retourner à la page d'accueil</a>

    <a href="{{ path('app_famous_dishes_edit', {'id': famous_dish.id}) }}">Modifier</a>

    {{ include('famous_dishes/_delete_form.html.twig') }}
{% endblock %}
