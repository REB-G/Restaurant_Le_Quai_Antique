{% extends 'base.html.twig' %}

{% block title %}FamousDishes{% endblock %}

{% block body %}
    <h1>FamousDishes</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ famous_dish.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ famous_dish.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ famous_dish.description }}</td>
            </tr>
            <tr>
                <th>ImageName</th>
                <td>{{ famous_dish.imageName }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_famous_dishes_index') }}">back to list</a>

    <a href="{{ path('app_famous_dishes_edit', {'id': famous_dish.id}) }}">edit</a>

    {{ include('famous_dishes/_delete_form.html.twig') }}
{% endblock %}
